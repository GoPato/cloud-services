type Data {
  dataId: ID
  content: String
}

type Query {
  listData: [Data]
}

type Mutation {
  createData(content: String!): Data
}

type Subscription {
  addedData: Data @aws_subscribe(mutations: ["createData"])
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
